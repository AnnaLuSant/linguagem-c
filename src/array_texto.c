#include <stdio.h>
#include <stdlib.h>

int main (){
    system ("clear");
    char nome[4] = "Joao";
    printf("%s\n",nome); //Utilizamos %s para strem que no caso e uma cadeia ao invés de um único caracter (%c)  
    printf("%c\n", nome[0]);


    return 0;
}